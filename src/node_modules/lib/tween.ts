import { gsap } from 'gsap'

const defaults = {
  targets: '',
}

function tween() {
  const set = (to: any) => {
    gsap.set(target, to);
  }

  const kill = (target: any) => {
    gsap.killTweensOf(target);
  }

  return {
    set,
    kill
  }
}

export { tween }