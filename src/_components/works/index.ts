import { defineComponent } from "lake";
import type { GlobalContext } from "@/_foundation/type";

export default defineComponent({
  name: "works",
  setup(_el, _props: GlobalContext) {
    //
  },
});
