import { css } from "@emotion/react";

export const Header = () => {
  return (
    <header className="" css={header}>
      <div data-component="backTo"></div>
    </header>
  );
};

const header = css``;
