import type { States, ZSet } from ".";

export function createThemeSlice() {
  const state = {
    //
  };

  const selectors = {
    //
  };

  const actions = (set: ZSet<States>) => ({
    //
  });

  return {
    state,
    selectors,
    actions,
  } as const;
}
